import { LIGHT_MODE, DARK_MODE } from "../types";

const setLightThemeStatus = {
  type: LIGHT_MODE,
};

const setDarkThemeStatus = {
  type: DARK_MODE,
};

export { setLightThemeStatus, setDarkThemeStatus };
